import styled from 'styled-components';
import { rem } from 'polished';

export const FormLabel = styled.label`
  width: 35%;
  color: #fff;
  font-weight: 600;
  height: ${rem(30)};
  padding: ${rem(10)};
  margin: ${rem(5)} auto;
`;